application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
demo.dir=${file.reference.mattnathan-demo}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Incubator.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.lib-mattnathan=../../lib/mattnathan
file.reference.mattnathan-beaninfo=beaninfo
file.reference.mattnathan-demo=demo
file.reference.mattnathan-java=java
file.reference.mattnathan-test=test
file.reference.SwingX-HEAD.jar=../../lib/mattnathan/SwingX-HEAD.jar
file.reference.swingx_painterbranch.jar=../../lib/mattnathan/swingx_painterbranch.jar
jar.compress=true
java.dir=${file.reference.mattnathan-java}
javac.classpath=\
    ${file.reference.swingx_painterbranch.jar}:\
    ${file.reference.SwingX-HEAD.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=true
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=org.jdesktop.swingx.JXEffectPanelDemo
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.mattnathan-beaninfo}
test.src.dir=${file.reference.mattnathan-test}
